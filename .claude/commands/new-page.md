Create a new documentation page.

Ask me for:
1. The parent section (directory under content/docs/)
2. The page title
3. The sidebar weight (or suggest one based on siblings)
4. A one-sentence description for the front matter

Then:
- Create the Markdown file with correct YAML front matter
- Add a brief placeholder structure: one `##` intro section and a `## Further
  reading` section with a placeholder link
- Show me the `hugo server` URL where the page will appear
- Do NOT write actual content yet; wait for my instructions
